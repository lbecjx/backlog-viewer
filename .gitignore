# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# local-backlog plugin files (https://github.com/lbecjx/local-backlog) —
# gitignored per this project's own choice, see local-backlog/.backlog-config.json
local-backlog/

# workflow-dev's own persistent context — private, per-machine, regenerable
# agent working memory, gitignored per this project's own choice, see
# .workflow-dev/config.json
.workflow-dev/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
